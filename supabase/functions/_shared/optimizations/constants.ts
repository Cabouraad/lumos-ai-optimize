/**
 * Shared constants for optimization generation system
 * Single source of truth for configuration values
 */

export const WINDOW_DAYS = 14;                        // standardize everywhere
export const LLM_MODEL = "gpt-4o-mini";
export const MAX_RETRIES = 3;
export const RETRY_BASE_MS = 400;
export const FINGERPRINT_ALGO = "SHA-256";
