import React from 'react';
import { Alert, AlertDescription } from '@/components/ui/alert';

export const EnvGate: React.FC = () => {
  // Since we have proper Supabase configuration, no need to show any warning
  return null;
};